# backend/data/mobile/

本目录预留给移动端思路核对的**正式**题目内容数据（角色 D 维护正文，schema/导入器由角色 C 维护）。

当前状态：C 尚未在 `docs/mobile-app/api/` 冻结协议，本目录暂不放正式数据，避免自行发明永久 schema。

第一阶段 3 道算法题样例的内容草稿与审核说明见 `docs/mobile-app/content/`（draft / pending C contract）。协议冻结后，草稿将按 C 的字段转换为正式数据放入本目录，并执行 C 提供的校验。
