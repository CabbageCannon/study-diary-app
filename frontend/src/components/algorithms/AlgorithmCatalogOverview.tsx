import type { AlgorithmCatalogOverview } from "../../types/algorithm";

export function AlgorithmCatalogOverview({ overview }: { overview: AlgorithmCatalogOverview }) {
  return <section className="algorithm-catalog-overview" aria-labelledby="catalog-overview-title"><div className="section-heading"><div><span className="pane-label">本地题库</span><h2 id="catalog-overview-title">题库概况</h2></div><strong className="tabular-number">{overview.total_problem_count}</strong></div><div className="catalog-metric-grid"><div><span>可用</span><strong className="tabular-number">{overview.active_problem_count}</strong></div><div><span>已完成</span><strong className="tabular-number">{overview.completed_problem_count}</strong></div><div><span>待复习</span><strong className="tabular-number">{overview.due_review_count}</strong></div><div><span>简单</span><strong className="tabular-number">{overview.difficulty_counts.easy ?? 0}</strong></div><div><span>中等</span><strong className="tabular-number">{overview.difficulty_counts.medium ?? 0}</strong></div><div><span>困难</span><strong className="tabular-number">{overview.difficulty_counts.hard ?? 0}</strong></div></div><p className="algorithm-muted-copy">题单 {Object.keys(overview.source_list_counts).length} 个 · 主要题型 {Object.keys(overview.topic_counts).slice(0, 4).join("、") || "待补充"}</p></section>;
}
